
name: clang.llvm-optrec
version: master

parent: base
src: mod.cpp
mode: shared
if_arg:
    win_shared: -DYAML_CPP_DLL
    shared: -DMKN_KUL_SHARED

profile:
- name: base
  dep: mkn.mod

- name: format
  mod: |
    clang.format{init{style: google, paths: .}}

- name: test
  parent: base
  mod: clang.llvm-optrec
  src: mod.cpp
